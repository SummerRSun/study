*** Settings ***
Resource          Keywords.txt
Resource          Variables.txt
Library           DatabaseLibrary

*** Test Cases ***
testcase1
    [Documentation]    写一个求和的keyword，并写一个case验证
    add    25    35

testcase2
    [Documentation]    将自己的名字，性别，年龄，学校的信息放到一个list中，然后遍历输出。
    @{list}    Create List    Summer    women    18    SunYuanUniversity
    : FOR    ${info}    IN    @{list}
    \    log    ${info}
    : FOR    ${name}    ${sex}    ${age}    ${university}    IN    Summer
    ...    women    18    SunYuanUniversity    Jack    men    20
    ...    YuanSunUniversity
    \    log    ${name}/${sex}/${age}/${university}

testcase3
    [Documentation]    写一个keyword功能为：输入分数，输出等级（60一下不及格，60-85良好。85以上优秀）
    ScoreGrade    36
    ScoreGrade    66
    ScoreGrade    86

testcase4
    [Documentation]    使用evaluate关键字，将字符串‘uyun’里的‘y’替换成‘u’
    ${oldchar}    set Variable    uyun
    log    ${oldchar}
    ${newchar}    Evaluate    '${oldchar}'.replace('y','u')
    log    ${newchar}
    Comment    ${replace}    Replace String    ${oldchar}    y    u
    Comment    log    ${replace}

testcase5
    [Documentation]    使用databaselibrary库，连接自己项目的数据库，并写个sql返回查询结果
    ${db_data}    连接mysql    select * from student    ${host}    ${username}    ${password}    ${dbname}
    log    ${db_data}
    ${rowcount}    Row Count    select * from student
    log    ${rowcount}
    关闭database
